import React from 'react';

const TodoCreate = () => {
    return (
        <form>
            <input type="text"/>
            <button type="submit">ADD</button>
        </form>
    );
};

export default TodoCreate;